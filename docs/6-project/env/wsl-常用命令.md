这里整理了最实用的WSL命令分类表，方便你快速查找：

## 📋 **WSL核心管理命令**

| 命令                               | 作用                        | 示例                                 |
| -------------------------------- | ------------------------- | ---------------------------------- |
| `wsl -l -v`                      | **查看已安装发行版**（名称、状态、WSL版本） | `wsl -l -v`                        |
| `wsl --shutdown`                 | **立即停止所有WSL发行版**（类似强制重启）  | `wsl --shutdown`                   |
| `wsl --terminate <发行版>`          | **停止指定发行版**               | `wsl --terminate Ubuntu-20.04`     |
| `wsl --set-default-version <版本>` | **设置新发行版的默认WSL版本**        | `wsl --set-default-version 2`      |
| `wsl --set-version <发行版> <版本>`   | **转换现有发行版的WSL版本**         | `wsl --set-version Ubuntu-20.04 2` |

## 🚀 **发行版启动与运行**

| 命令 | 作用 | 示例 |
|------|------|------|
| `wsl` | **启动默认发行版** | `wsl` |
| `wsl -d <发行版>` | **启动指定发行版** | `wsl -d Ubuntu-20.04` |
| `<发行版名>` | **直接使用发行版名称启动** | `ubuntu2004` |
| `wsl -d <发行版> -u <用户名>` | **以特定用户启动发行版** | `wsl -d Ubuntu-20.04 -u root` |
| `wsl -s <发行版>` | **设置默认发行版** | `wsl -s Ubuntu-20.04` |

## 💾 **备份、迁移与恢复**

| 命令 | 作用 | 示例 |
|------|------|------|
| `wsl --export <发行版> <文件路径>` | **导出发行版到tar文件** | `wsl --export Ubuntu D:\backup.tar` |
| `wsl --import <名称> <安装路径> <文件路径>` | **从tar文件导入发行版** | `wsl --import MyUbuntu D:\wsl D:\backup.tar` |
| `wsl --unregister <发行版>` | **注销（删除）发行版**（**数据会丢失**） | `wsl --unregister Ubuntu` |

## 📁 **文件系统与互操作**

| 命令 | 作用 | 示例 |
|------|------|------|
| `explorer.exe .` | **在Windows资源管理器中打开当前WSL目录** | `explorer.exe .` |
| `notepad.exe <文件>` | **用Windows记事本打开WSL文件** | `notepad.exe ~/.bashrc` |
| `/mnt/c/` | **访问Windows C盘** | `cd /mnt/c/Users/` |
| `wslpath -w <路径>` | **将WSL路径转换为Windows路径** | `wslpath -w ~/project` |

## ⚙️ **配置与诊断**

| 命令              | 作用                    | 示例              |
| --------------- | --------------------- | --------------- |
| `wsl --status`  | **显示WSL状态信息**（版本、内核等） | `wsl --status`  |
| `wsl --update`  | **更新WSL Linux内核**     | `wsl --update`  |
| `wsl --version` | **显示WSL版本信息**         | `wsl --version` |

## 🔄 **从Windows调用Linux命令**

| 命令 | 作用 | 示例 |
|------|------|------|
| `wsl <Linux命令>` | **在Windows中直接运行Linux命令** | `wsl ls -la` |
| `wsl <Linux命令> \| <Windows命令>` | **管道传递数据** | `wsl ls \| findstr ".txt"` |

## 💡 **实用技巧组合**

1. **优雅退出所有WSL并备份**：
   ```bash
   wsl --shutdown
   wsl --export Ubuntu-20.04 F:\wsl_backup\ubuntu_$(date +%Y%m%d).tar
   ```

2. **临时以root调试，然后恢复普通用户**：
   ```bash
   wsl -d Ubuntu-20.04 -u root  # 以root进入
   # 执行修复操作...
   exit                          # 退出
   ubuntu2004                    # 以普通用户重新进入
   ```

3. **在Windows中查看WSL目录**：
   ```bash
   # 在PowerShell中
   explorer.exe "\\wsl$\Ubuntu-20.04\home\你的用户名"
   ```

4. **创建快捷启动别名**（添加到`~/.bashrc`）：
   ```bash
   # 快速进入Windows目录
   alias winhome='cd /mnt/c/Users/你的用户名'
   # 用VS Code打开当前目录
   alias code='code .'
   ```

## 🚨 **注意事项**
- **数据安全**：`wsl --unregister` 会**永久删除**该发行版的所有数据，无回收站
- **路径格式**：Windows路径用反斜杠`\`，WSL内用正斜杠`/`
- **网络互通**：Windows可通过`localhost`访问WSL服务，WSL通过Windows IP访问宿主机

建议将常用命令保存为文档或添加到个人笔记中。如果你对某个命令有疑问或需要特定场景的用法，随时可以问我！